[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @morning-brief/agents build"]

[start]
cmd = "node packages/agents/dist/index.js run"
